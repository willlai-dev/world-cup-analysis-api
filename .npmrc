# Hoisted (flat) node_modules. Required for Prisma 6.7+ generated client:
# its default.js uses a `#main-entry-point` subpath import that Node fails to
# resolve when the client sits under pnpm's nested .pnpm/.../.prisma store.
node-linker=hoisted
auto-install-peers=true
strict-peer-dependencies=false
